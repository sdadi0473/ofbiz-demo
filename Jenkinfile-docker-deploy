pipeline {
    agent any
    triggers {
        pollSCM('*/5 * * * *')
    }
       stages {
       stage('Preparation') {
           steps {
            git 'https://github.com/RamachandraAnnadi/ofbiz-demo.git'
         }
       }
   stage('Code Analysis') {
                    steps {
                        gradlew('sonarqube')
                    }
                }
           }
        }
